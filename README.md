##
Run `bash download.sh` to download and decompress the data

## Adding data

For small files (say < 1000MB) just add to this repo

For large files, modify the `download.sh` script to download and add them
